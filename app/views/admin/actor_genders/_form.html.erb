<%= form_for [:admin, @actor_gender] do |f| %>
  <% if @actor_gender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@actor_gender.errors.count, "error") %> prohibited this actor_gender from being saved:</h2>

      <ul>
        <% @actor_gender.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :gender %><br />
    <%= f.text_field :gender %>
  </p>
  <p><%= f.submit %></p>
<% end %>
